version: '3'

services:
    middleware-pertamedika:
      build: .
      ports:
        - 80:8080
        - 443:8443
    mongodb:
      image: mongo:3.6-jessie
      volumes: 
        - ./data/db:/data/db
      restart: unless-stopped